function makeArray(firstArray, secondArray, maxLength) {
    // Change code below this line
let allArray = firstArray.concat(secondArray);
return allArray = allArray.slice(0,maxLength);

    // Change code above this line
  }

